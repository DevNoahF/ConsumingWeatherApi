spring:
  application:
    name: WeatherApiConsuming
    cors:
      allowed-origins: ${URL_CORS}:localhost:4200
  cache:
    type: redis
    redis:
      time-to-live: 5h
  data:
    redis:
      url: ${REDIS_URL:redis://localhost:6379}

